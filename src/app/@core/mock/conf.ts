export const api = {
    //base back end url, to specify in the index.html 
    url: window["cfgApiBaseUrl"]
};

export const title = {
    //base back end url, to specify in the index.html 
    value: window["title"]
};
